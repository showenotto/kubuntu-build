---
- name: Backing up zed...
  archive:
    path: "/.var/app/{{ APP_ID }}/config/zed"
    dest: "{{target_home_dir}}/kubuntu-build/roles/backup/zed/files/zed.tar.xz"
    format: xz
  register: config_archive_result
